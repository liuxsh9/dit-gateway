# Copy this file to .env and fill in values before running docker compose
#
# Required
SERVICE_TOKEN=
POSTGRES_PASSWORD=
DIT_DB_PASSWORD=
#
# Optional
# CORE_IMAGE=ghcr.io/liuxsh9/dit-core:latest
# GATEWAY_PORT=3000
#
# TLS (only needed with: docker compose --profile tls up -d)
# DOMAIN=data.example.com
